* [X] Barras de scroll laterales invisibles o quitarlas.
* [X] Dasboard no se actualiza al vuelo al tocar el teclado (al cambiar de páginas, o perfiles). El firmware avisa con `EV:CTX:<perfil>:<pagina>:<paginas>` en cuanto cambia cualquiera de las dos cosas, la venga de donde venga (su menú, el gestor de páginas, una tecla de salto o la propia app).
* [X] Al añadir icono a tecla, si no estás en la misma página, se envía el icono a la página que no es, en lugar de a la correcta. Los comandos de edición pasan ahora por un enganche que pone el teclado en la página que la app cree estar editando; el editor de iconos se queda con la página con la que se abrió y vuelve a ella antes de enviar.
* [X] Cuando le das a PLAY en una grabación, se tiene que poder configurar la velocidad a la que se reproduce, siendo la por defecto x3. de esta manera, por defecto, cuando grabas algo para repetirlo varias veces después, se reproduce mucho más rápido ahorrandote tiempo. además, quiero que se elimine el tiempo muerto antes de la primera acción (el tiempo que estás sin hacer nada justo al darle a record) y el tiempo muerto del final (el tiempo que estás sin hacer nada justo antes de darle a REC para finalizar la grabación)
* [X] Revisión de como persisten los datos. No me gusta que el PC tenga que tirar contínuamente de los datos del teclado, lo suyo es que tenga una copia, se genere un hash (y en el teclado tb) y así si no coincide con el hash del teclado sabes que hay diferencias y hay que sincronizar datos. Pero si abres la app debería cargar los datos correctamente sin tener el teclado conectado, pero no te deje editarlos hasta que conectes el teclado (para no generar discrepancias). El teclado resume su configuración con `GET_HASH` (un CRC32 por perfil y uno global, ver `include/config_hash.h`) y la app calcula la misma huella sobre su copia local (`OrbyGUI/src/hash.js`): al conectar solo se releen los perfiles cuya huella no cuadra. `tools/test/test_config_hash.cpp` + `.mjs` comprueban que las dos implementaciones no se desvían. Nada más sincronizar se descargan TODOS los iconos de todas las páginas con el comando nuevo `GET_OLED_PG`, así navegar entre perfiles y páginas ya no espera a nada. Sin el teclado conectado la app abre la copia en modo lectura: cartel fijo abajo a la izquierda y `requireDevice()` corta cualquier edición antes de tocar el modelo.
* [ ] Añadir una biblioteca de iconos para uso rápido y que los usuarios no tengan que crear los suyos.
* [ ] Añadir biblioteca de perfiles para que los usuarios no tengan que crear los suyos desde cero. Los usuarios pueden aportar al pool de perfiles en el apartado perfiles de la comunidad. Los usuarios tienen que aportar Nombre del perfil y descripción. EN la biblioteca de perfiles sale el número total de atajos, el número de páginas, etc.
* [ ] He pensado en monetizar perfiles o paquetes de perfiles: el software básico es gratis pero hay que pagar una baja cantidad de € para obtener acceso a un paquete de perfiles para unos cuantos programas como Altium, excel, word, etc, o pagar cada uno por separado. Estudiar viavilidad de esto.
* [ ] La app empieza a cobrar forma, empieza a ser algo que sí usaría a diario y si pagaria por ello. Así que hay que empezar a pensar en como certificar la aplicación o algo así, para que no te salte un aviso de windows al intentar instalarla. Además hay que hacer una página web para poder publicitar esto y venderlo, una landing page para un kickstarter o algo así. Tambien que el firmware del teclado no parezca extraño al ordenador la primera vez que lo conectas, que se comporte como un producto legítimo. Evaluar que es todo lo que hace falta.
